// Centralize icon imports
import { 
  Brain, 
  Sparkles, 
  Clock, 
  Lightbulb, 
  AlertTriangle,
  Send,
  Search,
  GanttChart // Using GanttChart instead of Impact which doesn't exist
} from 'lucide-react';

export const Icons = {
  Brain,
  Sparkles,
  Clock,
  Lightbulb,
  AlertTriangle,
  Send,
  Search,
  Impact: GanttChart // Alias GanttChart as Impact for backwards compatibility
};
